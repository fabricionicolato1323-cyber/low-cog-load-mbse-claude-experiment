# Native modules ship prebuilt binaries (better-sqlite3) or use optional-dependency binaries (esbuild), so no install script is needed.
# Without this, npm >= 11.19 on a compiler-less Linux runs an implicit `node-gyp rebuild` for better-sqlite3 and `npm ci` fails (Slice 0 finding).
ignore-scripts=true
